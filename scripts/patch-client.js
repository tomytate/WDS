const fs = require('fs'); let c = fs.readFileSync('packages/db/src/client.ts', 'utf8'); c = c.replace(/export async function closeDb\(\) \{[\s\S]*?\}/, 'let adminSupabaseInstance: any = null;\n\nexport async function closeDb() {\n  supabaseInstance = null;\n  adminSupabaseInstance = null;\n}\n\nexport function getAdminDb(): any {\n  if (!process.env.NEXT_PUBLIC_SUPABASE_URL || !process.env.SUPABASE_SERVICE_ROLE_KEY) { throw new Error(\'Missing config\'); }\n  if (!adminSupabaseInstance) {\n    adminSupabaseInstance = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false } });\n  }\n  return adminSupabaseInstance;\n}\n'); fs.writeFileSync('packages/db/src/client.ts', c);
